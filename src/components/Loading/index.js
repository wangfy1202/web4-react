/**
 * loading component
 */
import React from "react";

export default function Loading() {
  return (
    <div
      style={{
        display: "flex",
        width: "100%",
        height: "100%",
        justifyContent: "center",
        alignItems: "center",
        color: "#999",
        fontSize: "0.5rem"
      }}
    >
      Loading
    </div>
  );
}
